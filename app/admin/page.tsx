'use client';
import {useEffect,useState} from 'react';
import {onAuthStateChanged} from 'firebase/auth';
import {auth} from '../../lib/firebase';
export default function Admin(){const[u,setU]=useState<any>(null);useEffect(()=>onAuthStateChanged(auth,setU),[]);return <main className="wrap"><h1>Admin Console</h1><p className="muted">Signed in as {u?.email||'checking authentication…'}</p><div className="grid"><div className="card"><h3>Users</h3><div className="stat">—</div></div><div className="card"><h3>Active Licenses</h3><div className="stat">—</div></div><div className="card"><h3>Credits Sold</h3><div className="stat">—</div></div><div className="card"><h3>Revenue</h3><div className="stat">—</div></div></div><div className="card" style={{marginTop:16}}><h2>Admin capabilities</h2><ul><li>Create / block / unblock / revoke licenses</li><li>Add or remove credits with audit records</li><li>Change device limits and reset device activations</li><li>Manage credit packages and global settings</li><li>Review payments, usage and audit logs</li></ul><p className="muted">Production admin actions must be wired to the included Firebase Functions and protected by Firebase custom claims. Never trust a frontend-only admin flag.</p></div></main>}
